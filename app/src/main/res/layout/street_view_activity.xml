<?xml version="1.0" encoding="utf-8"?>
<!--
  【核心修正】
  1. 使用 androidx.constraintlayout.widget.ConstraintLayout 作为根布局。
  2. 确保 FragmentContainerView 是它的直接子元素。
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
      这个Fragment现在是ConstraintLayout的子元素，
      因此所有 app:layout_constraint... 属性都是有效的。
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/street_view_panorama_fragment"
        android:name="com.google.android.gms.maps.SupportStreetViewPanoramaFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>